{
  "name": "puraloka-suite",
  "version": "1.0.0",
  "description": "ERP konstruksi multi-tenant — Puraloka Persada",
  "main": "index.js",
  "scripts": {
    "bootstrap": "node scripts/bootstrap.mjs",
    "dev:api": "pnpm --filter @puraloka/api dev",
    "dev:web": "pnpm --filter web dev",
    "test": "pnpm --filter @puraloka/api test",
    "test:web": "pnpm --filter web test",
    "typecheck": "pnpm --filter @puraloka/api exec tsc --noEmit && pnpm --filter web typecheck",
    "lint:ratchet": "pnpm --filter @puraloka/api lint:ratchet && pnpm --filter web lint:ratchet",
    "build": "pnpm --filter @puraloka/api build && pnpm --filter web build",
    "db": "node scripts/db/introspect.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "_catatan_packageManager": [
    "══════════════════════════════════════════════════════════════════════",
    "KENAPA ADA DUA DEKLARASI PENGELOLA PAKET DI BERKAS INI",
    "══════════════════════════════════════════════════════════════════════",
    "",
    "`devEngines.packageManager` di bawah sudah ada lebih dulu — format npm",
    "11+, dibaca saat menjalankan skrip di mesin pengembang.",
    "",
    "`packageManager` (Corepack) ditambahkan 2026-09-01 karena EAS Build",
    "TIDAK membaca `devEngines`. Tanpa itu server Expo memakai pnpm",
    "bawaannya, dan build GAGAL:",
    "",
    "    We detected that 'apps/mobile' is a pnpm workspace",
    "    Running \"pnpm install --frozen-lockfile\"",
    "    ERR_PNPM_BROKEN_LOCKFILE  The lockfile is broken:",
    "      expected a single document in the stream, but found more",
    "",
    "Lockfile-nya TIDAK rusak. pnpm 11 menulisnya sebagai DUA dokumen YAML",
    "— dokumen pertama `packageManagerDependencies`, fitur pnpm 11 — dan",
    "pnpm versi lama menolak bentuk itu. Diukur: `lockfileVersion` muncul 2x",
    "sejak commit f9954c77, dan `pnpm install --frozen-lockfile` LOKAL",
    "(pnpm 11.8.0) menerimanya tanpa keluhan.",
    "",
    "Jadi yang salah bukan lockfile-nya melainkan pnpm yang membacanya.",
    "",
    "⚠ KEDUANYA HARUS SAMA VERSINYA. Kalau salah satu dinaikkan tanpa yang",
    "lain, mesin pengembang dan server build memakai pnpm berbeda — dan",
    "bedanya baru terlihat saat build gagal, bukan saat menyunting."
  ],
  "packageManager": "pnpm@11.8.0",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.8.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "axe-core": "^4.12.1"
  },
  "engines": {
    "node": ">=20"
  }
}
